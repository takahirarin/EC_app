<div class="col-md-2">
    ジャンル検索
    <table border="1">
      <tbody>
        <% genres.each do |genre| %>
        <tr>
          <td><%=link_to genre.name, search_path(genre) %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    </div>